export type BlogType = {
    id: number
    slug: string
    category: string
    tags: string[]
    title: string
    credit?: string
    posted_on: string
    read: string
    intro: string
    image?: string
    body: string
    author: string  
}
